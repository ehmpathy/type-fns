# 5.1 execution — `HasMaybe<T, K>`

## todos

- [x] create `src/types/HasMaybe.ts` — the type (guard omitted per vision)
- [x] add `export * from './types/HasMaybe'` to `src/index.ts` (alphabetical, after `Empty`)
- [x] create `src/types/HasMaybe.test.ts` — tests A/B/C + interface-constraint case
- [x] validate: `interface`-typed input satisfies the bound — **held, no change needed** (`interface SurferRef` compiles against `Record<string, any>`; typecheck green)
- [x] run typecheck — passed
- [x] run unit tests — 4/4 passed
- [x] run format — passed
- [x] run lint — passed

## outcome

- **must-validate gate resolved**: the `interface` vs `Record<string, any>` risk did **not** materialize. `interface SurferRef { role; exid }` satisfies the bound as-is; the constraint stays `Record<string, any>` per the wish, no fallback applied.
- **assertions are structural** (value assignability + `@ts-expect-error`), not hover-string matches, per vision.
- **guard omitted** per vision decision.

## peer-review convergence (iteration i003)

peer reviewers (behavior-intent-coverage, ergo-friction-hazards) flagged 3 blockers: the vision's three documented guarantees (composability inverse, no-op-when-optional, `Partial<T>`-when-`K=keyof T`) were prose-only, not tested. this overrode my earlier vision self-review that had scoped the inverse as an "optional" test — the reviewers correctly applied verification-strictness: a documented guarantee must be proven.

- [x] added test: composability inverse `HasMetadata<HasMaybe<Wave,'id'>,'id'>` round-trips to `Wave`
- [x] added test: no-op when key already optional
- [x] added test: `Partial<T>` equivalence when `K = keyof T`
- [x] unit: **7 passed** / 0 failed / 0 skipped; types green; format green

note: `lint --what deps` flags `rhachet-brains-fireworksai` as an unused devDependency — this is from the human's brain-install (`package.json` was pre-modified before this behavior), orthogonal to the HasMaybe change (biome passed clean on all 47 files).

## decisions carried from vision

- **placement**: `src/types/HasMaybe.ts` (pure type → `src/types/*` convention)
- **guard**: omit `hasMaybe` (widen-only guard can never return `false`)
- **name**: `HasMaybe`
- **signature**: `Omit<T, K> & Partial<Pick<T, K>>`
- **must-validate**: `interface` vs `Record<string, any>` bound; assert structurally not by hover
