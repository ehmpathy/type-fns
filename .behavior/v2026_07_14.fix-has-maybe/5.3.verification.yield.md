# 5.3 verification — `HasMaybe<T, K>`

## verification checklist

### behavior coverage (vs wish + vision)

no `repros` / blackbox-criteria artifact was declared for this behavior (a pure type utility). coverage is mapped against the wish's `.contract` + `.tests to add` and the vision's documented guarantees.

| behavior (from wish/vision) | test | status |
|-----------------------------|------|--------|
| make listed key `K` optional; other keys stay required | `HasMaybe.test.ts` — `should make the listed key optional while other keys stay required` | ✅ |
| multi-key `K1 \| K2` both optional, others untouched | `should make multiple listed keys optional, others untouched` | ✅ |
| `K` not a key of `T` → compile error | `should reject a key that is not a key of T` (`@ts-expect-error`) | ✅ |
| `interface`-typed `T` satisfies the bound (vision must-validate) | `should accept an interface-typed T (not just a type alias)` | ✅ |
| composability inverse: `HasMetadata<HasMaybe<T,K>,K>` ≡ `T` | `should compose with HasMetadata as an exact inverse on the same key` | ✅ |
| no-op when `K` already optional | `should be a no-op when the key is already optional` | ✅ |
| `K = keyof T` ≡ `Partial<T>` | `should be equivalent to Partial<T> when K = keyof T` | ✅ |

every promised behavior has a test. no behavior left untested.

### zero skips verified
- [x] no `.skip()` or `.only()` found — grep of `HasMaybe.test.ts` shows plain `describe`/`it`
- [x] no silent credential bypasses — pure type test, no credentials involved
- [x] no prior failures carried forward — full unit suite green (15/15)

### snapshot coverage for contract outputs

**not applicable.** `HasMaybe` is a compile-time type utility in a type-fns library. it has no CLI stdout, no API response, no app screen, no SDK runtime return value — no output artifact to snapshot. verification is via type-level assertions (`@ts-expect-error`) + value assignability, per the vision's "assert structurally" directive.

- [x] no cli command added → no `.snap` owed
- [x] no app screen added → no `.snap` owed
- [x] no sdk runtime method added → no `.snap` owed

### snapshot change rationalization

**none** — no `.snap` files added, modified, or deleted in this change.

### tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, "passed (3s)", no errors |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, "passed (1s)", no errors |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, "passed (4s)" — biome clean on 47 files + depcheck + cycles all pass |
| unit | `rhx git.repo.test --what unit --mode apply` | ✅ | exit 0, "15 passed, 0 failed, 0 skipped" across 4 suites |
| integration | n/a | — | no `.integration.test.ts` files exist in repo (pure type library) |
| acceptance | n/a | — | no `.acceptance.test.ts` files exist in repo (pure type library) |

- [x] every applicable test command was run (not assumed)
- [x] every output was observed
- [x] exact exit code verified (0 = pass)
- [x] no tests skipped, mocked, or faked

### lint fix applied during buttonup

lint initially failed: `depcheck` flagged `rhachet-brains-fireworksai` as an unused devDependency. root cause: this brain package (installed so the peer reviewers could run) is loaded by rhachet at **runtime**, not via a static import, so depcheck cannot see it. its two sibling brain packages — `rhachet-brains-anthropic` and `rhachet-brains-xai` — were already in `.depcheckrc.yml`'s ignore list; `fireworksai` was simply omitted. **fix**: added `rhachet-brains-fireworksai` to `.depcheckrc.yml` alongside its siblings (consistent with the established pattern). lint re-run → green.

### contract output snapshot exhaustiveness

**not applicable** — no user-faced contract (cli/api/sdk) in this change. `HasMaybe` is a compile-time type. its "caller experience" is the type-checker's behavior, verified exhaustively by the 7 tests above (positive paths, negative `@ts-expect-error` paths, and edge cases: no-op, `Partial<T>`, interface-typed).

### blockers
- none.
