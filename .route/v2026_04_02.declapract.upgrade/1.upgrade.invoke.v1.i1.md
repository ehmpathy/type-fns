# 1.upgrade.invoke — completion report

## status

✅ upgrade completed with repairs

## summary

declapract upgrade from `0.12.3` → `0.13.16` and `declapract-typescript-ehmpathy` from `0.39.9` → `0.47.58` completed successfully after fixing several defects.

## defects repaired

### 1. biome lint errors (source code)

| file | issue | fix |
|------|-------|-----|
| `src/checks/hasMetadata.ts` | `noExplicitAny` on generic constraints | added biome-ignore comments (required for flexible generics) |
| `src/checks/hasMetadata.ts` | format issue with inline comment | moved comment to separate line |
| `src/checks/hasMetadata.test.ts` | unused variable `pot` | prefixed with `_` |
| `src/types/PickOne.test.ts` | empty object pattern `{}` | changed to `_input` |
| `provision/github.repo/resources.ts` | `noExplicitAny` on `pkg as any` | created `PackageJson` interface and typed cast |

### 2. cycle detection (infrastructure defect)

**issue**: the `test:lint:cycles` command used `--exclude '^$'` which only matches empty string, not node_modules. this caused dpdm to find cycles in dependencies (helpful-errors → type-fns → helpful-errors).

**fix**: changed exclude pattern to `'node_modules'` in package.json.

**note**: this is an infrastructure defect in `declapract-typescript-ehmpathy`. the exclude pattern should properly exclude node_modules from cycle detection.

### 3. depcheck false positives

added to `.depcheckrc.yml` ignores:
- `type-fns` (self-reference for testing)
- `@tsconfig/node-lts-strictest`
- `core-js`
- `rhachet-brains-xai`
- `ts-jest`
- `ts-node`
- `@jest/globals` (transitive from jest)

### 4. type errors

| file | issue | fix |
|------|-------|-----|
| `src/types/ArrayWith.ts` | type not exported | added `export` |
| `src/types/ArrayWith.test.ts` | missing import | added import statement |
| `blackbox/environment.ts` | imports non-existent module | deleted file (template artifact) |

## infrastructure feedback

the following issues should be addressed in `declapract-typescript-ehmpathy`:

1. **cycle detection exclude pattern**: `--exclude '^$'` should be `--exclude 'node_modules'`
2. **depcheck ignores**: common transitive dependencies like `@jest/globals` should be pre-ignored
3. **template artifacts**: `blackbox/environment.ts` was created but references non-existent `src/utils/environment` module

## verification

all tests pass:
- test:commits ✅
- test:types ✅
- test:format ✅
- test:lint ✅
- test:unit ✅
- test:integration ✅
- test:acceptance:locally ✅
