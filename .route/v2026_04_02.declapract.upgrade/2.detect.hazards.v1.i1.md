# 2.detect.hazards — hazard report

## summary

no critical hazards detected. all tests pass locally after repairs.

## hazards analyzed

### 1. test hazards

| change | risk | status |
|--------|------|--------|
| jest 29 → 30 | medium | ✅ verified — tests pass |
| test-fns 1.6.0 → 1.15.7 | medium | ✅ verified — tests pass |
| biome lint rules (stricter than eslint) | low | ✅ repaired in stone 1 |
| cycle detection added | low | ✅ repaired in stone 1 |

**verification**: `THOROUGH=true npm run test` passes all 66 tests.

### 2. cicd hazards

| change | risk | status |
|--------|------|--------|
| npm → pnpm in workflows | medium | ⚠️ verify in ci |
| install workflow rewritten | medium | ⚠️ verify in ci |
| new `.declastruct.yml` workflow | low | n/a (new feature) |
| new `provision.yml` workflow | low | n/a (new feature) |
| cache key format changed | low | ⚠️ first run will miss cache |

**note**: ci may take longer on first run due to cache miss from key format change.

### 3. path hazards

| change | impact | status |
|--------|--------|--------|
| `acceptance/` → `blackbox/` | low | ✅ fixed — removed broken template file |
| `src/domain/` → `src/domain.objects/` | low | ✅ verified — no imports broke |
| `.depcheckrc.yml` path (added dot prefix) | low | ✅ verified — depcheck runs |

### 4. config hazards

| change | impact | status |
|--------|--------|--------|
| node v20.12.2 → v22.21.0 | medium | ✅ verified — tests pass locally |
| `test:types` uses `tsconfig.json` instead of `tsconfig.build.json` | low | ✅ verified — types check passes |
| tsconfig extends: string → array | low | ✅ verified — build compiles |
| tsconfig module: added node16 | low | ✅ verified — tests pass |
| eslint deleted, biome added | low | ✅ verified — lint passes |
| prettier deleted | none | n/a |
| `packageManager` field added | low | ⚠️ may require pnpm installation on ci runners |

## recommendations

1. **monitor first ci run**: watch for pnpm-related issues or cache misses
2. **verify github secrets**: new workflows may need `GITHUB_TOKEN` with correct permissions for declastruct

## conclusion

all test hazards have been repaired and verified locally. cicd hazards are low-risk and will be verified when the pr runs in ci.
